import 'package:flutter/material.dart';

class PhotoLoadingCard extends StatelessWidget {
  const PhotoLoadingCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 120,
      width: 120,
      color: Colors.grey[300],
    );
  }
}
